# Core data stack — pinned to satisfy recmetrics constraints
numpy==1.26.4
pandas==1.5.3          # recmetrics requires <2.0.0
matplotlib==3.7.3
seaborn==0.11.2        # recmetrics requires <0.12.0
plotly==4.14.3         # recmetrics requires <5.0.0

# recmetrics manual dependency (install before recmetrics)
funcsigs==1.0.2

# recmetrics itself — installed --no-deps to avoid resolver conflicts
# run: pip install --no-deps recmetrics

# Recommender system
scikit-surprise

# ML
scikit-learn==1.3.2

# Utilities
tqdm
kaggle
ipykernel
nbformat
